<template>
  <main class="page-shell">
    <header class="sticky top-0 z-10 h-[54px] bg-white shadow-sm">
      <div class="relative mx-auto flex h-full w-[1200px] items-center justify-center">
        <h1 class="text-[20px] font-bold text-gray-900">差旅费用报销单</h1>
        <div class="absolute right-0 text-sm text-gray-700">提单日期 待接入</div>
      </div>
    </header>

    <section class="mx-auto my-4 min-h-[640px] w-[1200px] bg-white p-5">
      <div class="flex h-[520px] items-center justify-center text-gray-500">
        报销单详情页面骨架已就绪，后续阶段逐步还原各分区。
      </div>
    </section>

    <footer class="fixed bottom-0 left-0 right-0 z-10 h-[54px] bg-white shadow-[0_-2px_10px_rgba(0,0,0,0.06)]">
      <div class="mx-auto flex h-full w-[1200px] items-center justify-center gap-3">
        <el-button>关闭</el-button>
        <el-button type="primary">提交</el-button>
      </div>
    </footer>
  </main>
</template>
